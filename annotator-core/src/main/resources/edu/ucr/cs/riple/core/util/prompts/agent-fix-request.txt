I want to resolve an error reported by NullAway.
The error is of the type "%s" and the full error message is: "%s".

The error is in file "%s".
The line with the error is line %d: "%s".
The error is within %s.

%s
Your fix should address only the specific error mentioned above.

Verifying a created fix:

1.) After having made a change, inspect the changed file to check for any syntax errors that you might have introduced. 
    (e.g.: `nl -ba filename.py | sed -n '10,20p'`)
2.) Then run the gradle build, save it to a file and grep the file for the target error's file path to see if the target NullAway error still exists or if some other error was introduced in that file.
    To do so, use the following command: `./gradlew build -x test -q > build_output.log 2>&1 || grep -A 5 -B 5 '%s' build_output.log`