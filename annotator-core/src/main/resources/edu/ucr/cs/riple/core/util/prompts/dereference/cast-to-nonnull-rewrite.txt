In the method below, the expression %s is not nullable at the point it is used.

```java
%s
```
Please rewrite the method so that at the line "%s", "%s" is cast to Nonnull using `Nullability.castToNonnull(%s)`.    
See the example below, for expression `foo`, where foo is a false positive Nullable expression, wrap it with `Nullability.castToNonnull(foo).getBar()`.

```java
public t(){
    String l = this.foo.getBar();
}
```

will be:
```java
public t(){
    String l = Nullability.castToNonnull(this.foo).getBar();
}
```

If "%s" is a local variable, defined at an earlier line in the method, apply the cast there instead, to avoid multiple casts.  
See the example below, for expression `foo`, where foo is a false positive Nullable expression defined at line 2, use `Nullability.castToNonnull(getFoo())` to cast it at its definition.  
```java
public t2(){
    Object foo = getFoo();
    String l = foo.getBar();
}
```

will be:
```java
public t2(){
    Object foo = Nullability.castToNonnull(getFoo());
    String l = foo.getBar();
}
```

### Requirements:
- Do not import any additional libraries or classes
- Do not add Java comments, but also do not remove any preexisting comments.
- Only modify relevant lines as required
- Do not address any other issues
- Return only the modified method wrapped in the XML format shown below.

Place the Java code inside the <code> tag using a CDATA block with ```java formatting.

Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block.
<response>
  <success>true</success>
  <code><![CDATA[
  ```java
  Your fixed code snippet here
  ```
    ]]>
  </code>
</response>
